_schema-version: '3.1'
ID: RiskMgmt
version: 1.0.0
description: A simple CAP project.
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
build-parameters:
  before-all:
    - builder: custom
      commands:
        - rm -rf build/dist
        - mkdir -p build/dist
        - rm -rf build/ui-deployer
        - mkdir -p build/ui-deployer
    - builder: custom
      commands:
        - npm install --production
        - >-
          bash -c "cds -v | grep 'cds-dk' || npm i --production --no-save
          @sap/cds-dk @sap/mdk-tools"
        - bash -c "export PATH=$PATH:$(pwd)/node_modules/.bin"
        - cds build --production
modules:
  - name: RiskMgmt-RiskMgmt.Risks
    type: html5
    path: app/RiskMgmt.Risks
    build-parameters:
      builder: custom
      commands:
        - rm -rf dist-zip dist
        - >-
          npx -p @ui5/cli@^3.0.0 ui5 build --dest dist
          --include-task=generateManifestBundle
          --include-task=generateCachebusterInfo
        - mkdir dist-zip
        - bash -c "cd dist && npx bestzip ../dist-zip/RiskMgmtRisks.zip *"
      _artifactName: RiskMgmtRisks.zip
      supported-platforms: []
      build-result: dist-zip
  - name: RiskMgmt-db-deployer
    type: hdb
    path: gen/db
    parameters:
      buildpack: nodejs_buildpack
    build-parameters:
      builder: npm
      ignore:
        - node_modules
        - package-lock.json
    requires:
      - name: RiskMgmt-service-uaa
      - name: RiskMgmt-service-db
  - name: RiskMgmt-srv
    type: nodejs
    path: gen/srv
    parameters:
      memory: 256MB
    provides:
      - name: srv-api
        properties:
          srv-url: ${default-url}
    build-parameters:
      builder: npm
      ignore:
        - node_modules
        - package-lock.json
    requires:
      - name: RiskMgmt-service-uaa
      - name: RiskMgmt-service-db
      - name: RiskMgmt-service-destination
      - name: RiskMgmt-service-metadata
      - name: RiskMgmt-service-connectivity
      - name: RiskMgmt-service-logs
  - name: RiskMgmt-aux-destinations
    type: com.sap.application.content
    parameters:
      content:
        subaccount:
          existing_destinations_policy: update
          destinations:
            - Authentication: OAuth2UserTokenExchange
              Name: RiskMgmt-service-uaa
              ServiceInstanceName: RiskMgmt-uaa
              ServiceKeyName: RiskMgmt-uaa-key
              sap.cloud.service: lcap.RiskMgmt
            - Authentication: OAuth2UserTokenExchange
              Name: RiskMgmt-app-srv
              TokenServiceInstanceName: RiskMgmt-uaa
              TokenServiceKeyName: RiskMgmt-uaa-key
              URL: ~{srv-api/srv-url}
              sap.cloud.service: lcap.RiskMgmt
            - Name: RiskMgmt-service-html5-apps-repo-host
              ServiceInstanceName: RiskMgmt-html5-apps-repo-host
              ServiceKeyName: RiskMgmt-html5-apps-repo-host-key
              sap.cloud.service: lcap.RiskMgmt
            - Authentication: OAuth2UserTokenExchange
              Name: RiskMgmt-RiskMgmtService
              TokenServiceInstanceName: RiskMgmt-uaa
              TokenServiceKeyName: RiskMgmt-uaa-key
              URL: ~{srv-api/srv-url}/service/RiskMgmt
              sap.cloud.service: lcap.RiskMgmt
              sap.applicationdevelopment.actions.enabled: false
              sap.build.usage: CAP
              sap.processautomation.enabled: false
              HTML5.DynamicDestination: false
              WebIDEEnabled: false
              MobileEnabled: false
              AppgyverEnabled: false
    build-parameters:
      no-source: true
    requires:
      - name: RiskMgmt-service-uaa
        parameters:
          service-key:
            name: RiskMgmt-uaa-key
      - name: srv-api
      - name: RiskMgmt-service-html5-apps-repo-host
        parameters:
          service-key:
            name: RiskMgmt-html5-apps-repo-host-key
      - name: RiskMgmt-service-destination
        parameters:
          content-target: true
  - name: RiskMgmt-launchpad
    type: html5
    path: build/launchpad
    build-parameters:
      builder: custom
      commands:
        - npm install
        - >-
          npx -p @ui5/cli@^3.0.0 ui5 build --dest dist
          --include-task=generateManifestBundle
          --include-task=generateCachebusterInfo
        - bash -c "cd dist && npx bestzip ../../../build/dist/launchpad.zip *"
        - rm -rf dist
      _artifactName: launchpad.zip
      supported-platforms: []
      build-result: ../../build/dist
  - name: RiskMgmt-aux-ui-deployer
    type: com.sap.application.content
    path: build/ui-deployer
    build-parameters:
      build-result: resources
      requires:
        - name: RiskMgmt-RiskMgmt.Risks
          artifacts:
            - RiskMgmtRisks.zip
          target-path: resources
        - name: RiskMgmt-launchpad
          artifacts:
            - launchpad.zip
          target-path: resources
    requires:
      - name: RiskMgmt-service-html5-apps-repo-host
        parameters:
          content-target: true
resources:
  - type: org.cloudfoundry.managed-service
    name: RiskMgmt-service-uaa
    parameters:
      service: xsuaa
      service-plan: application
      service-name: RiskMgmt-uaa
      path: xs-security.json
      config:
        oauth2-configuration:
          redirect-uris:
            - https://*.${default-domain}/login/*
        xsappname: RiskMgmt-${space}
        role-collections:
          - description: ''
            name: RiskMgmt-RiskViewer-${space}
            role-template-references:
              - $XSAPPNAME.RiskViewer
          - description: ''
            name: RiskMgmt-RiskManager-${space}
            role-template-references:
              - $XSAPPNAME.RiskManager
  - type: org.cloudfoundry.managed-service
    name: RiskMgmt-service-db
    parameters:
      service: hana
      service-plan: hdi-shared
      service-name: RiskMgmt-db
  - type: org.cloudfoundry.managed-service
    name: RiskMgmt-service-destination
    parameters:
      service: destination
      service-plan: lite
      service-name: RiskMgmt-destination
      config:
        HTML5Runtime_enabled: true
        version: 1.0.0
  - type: org.cloudfoundry.user-provided-service
    name: RiskMgmt-service-metadata
    parameters:
      service: metadata
      service-plan: standard
      service-name: RiskMgmt-metadata
      config:
        default-domain: ${default-domain}
  - type: org.cloudfoundry.managed-service
    name: RiskMgmt-service-connectivity
    parameters:
      service: connectivity
      service-plan: lite
      service-name: RiskMgmt-connectivity
  - type: org.cloudfoundry.managed-service
    name: RiskMgmt-service-logs
    parameters:
      service: application-logs
      service-plan: lite
      service-name: RiskMgmt-logs
  - type: org.cloudfoundry.managed-service
    name: RiskMgmt-service-html5-apps-repo-host
    parameters:
      service: html5-apps-repo
      service-plan: app-host
      service-name: RiskMgmt-html5-apps-repo-host
